services:
  - type: web
    name: quant-trading-model
    env: docker
    dockerfilePath: ./Dockerfile
    # Docker builds from Dockerfile; use no-op if build command field is required
    buildCommand: "true"
